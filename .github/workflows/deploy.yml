name: Pull Request Trigger Test

on:
  pull_request:
    branches:
      - main

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World! The trigger is working!"